<UserControl x:Class="WpfCustomApplication.TabPager"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfCustomApplication"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
  <UserControl.Resources>
    <DataTemplate x:Key="TabContentTemplate">
      <UserControl Content="{Binding View}"/>
    </DataTemplate>
    <DataTemplate x:Key="TabHeaderTemplate">
      <StackPanel Orientation="Horizontal">
        <UserControl Content="{Binding Title}"/>
        <Button Margin="5,0,0,0" Width="15" Visibility="{Binding CanClose, Converter={StaticResource BoolToHiddenConverter}}"
                Command="{Binding CloseCommand}"/>
      </StackPanel>
    </DataTemplate>
  </UserControl.Resources>
  <Grid>
    <TabControl x:Name="TabElement" TabStripPlacement="Left"
                ItemsSource="{Binding AllTabPages}" 
                ItemTemplate="{DynamicResource TabHeaderTemplate}" 
                ContentTemplate="{DynamicResource TabContentTemplate}" >
    </TabControl>
  </Grid>
</UserControl>

1) Автоматизация изменения состояния LoadingControl (IsBusy, IsOnHold...)
2) TaskWithPriority.Factory implementation
3) CommandManager.InvalidateRequerySuggested() для отдельных комманд
4) Event в static объекте
-) когда вызывается ConvertBack? - When a value propagates from the binding target to the binding source
6) в каком потоке запускается ContinueWith
7) как работают кривые Безье и как из них сделать squircle

TaskContainer:

3) в отдельную библиотеку?